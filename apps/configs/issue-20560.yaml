apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: issue-20650
  namespace: argocd
spec:
  destination:
    namespace: issue-20650
    server: https://kubernetes.default.svc
  project: default
  source:
    path: apps/manifests/issue-20560
    repoURL: https://github.com/alleaffengaffen/banana-bread
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true
